# Local-dev default; src/config/env.ts falls back to this same value if unset.
# The deployed (Render) origin is set as a real env var in Vercel's project
# config, never committed here.
VITE_API_BASE_URL=http://localhost:8000
