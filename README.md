# SkyCast

Weather application with a TypeScript frontend and a FastAPI backend.

## Backend

```bash
cd backend
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest
```

## Frontend

```bash
cd frontend
npm install
npm test
```
